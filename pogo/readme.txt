1. install python 2.7
2. install pip
	python get-pip.py
3. update pip
	Linux or OS X
		pip install -U pip
	windows
		python -m pip install -U pip

4. Install all requirements for the project
	pip install -r requirements.txt

python ./main.py -a "google" -u "your_account@**.com" -p "your_password" -l "location example Beach Street & The Embarcadero, San Francisco, CA" 
